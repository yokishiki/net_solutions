Для backend'а в переменных окружения устанавливается имя базы данных в `DB_NAME`.

Backend считает, что frontend запускается на портах 5173 и 4173, это захардкожено.

Аналогично, frontend считает, что backend на 8000 порту localhost'а.

Зависимости backend'а указаны в `requrements.txt`.